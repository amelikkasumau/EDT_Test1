
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если Объект.Ссылка.Пустая() Тогда
		ПоказатьПредупреждение(, "Тест!");
		Сообщить("Ура!!!");
	КонецЕсли;
	Сер();
КонецПроцедуры

Процедура Сер()
	Запрос = Новый Запрос();
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ПРЕДСТАВЛЕНИЕССЫЛКИ(Товары.Ссылка) КАК Наим
	|ИЗ
	|	Справочник.Товары КАК Товары";
	Выборка = Запрос.Выполнить().Выбрать();
	Пока Выборка.Следующий() Цикл
		Сообщить(Выборка.Наим);		
	КонецЦикла;
КонецПроцедуры